{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "watch": "lerna bootstrap && lerna run watch --stream"
  },
  "devDependencies": {
    "lerna": "^3.17.0"
  }
}
